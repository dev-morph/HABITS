version: 0.0
os: linux
files:
  - source: /habits
    destination: /app/habits
    overwrite: yes
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755
hooks:
  AfterInstall:
    - location: ./loadImage.sh
      timeout: 60
      runas: root